apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cloudvibe-database-operator.name" . }}
  labels:
    {{- include "cloudvibe-database-operator.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.serviceAccount.annotations | nindent 4 }}
